# 计划：追平 OrbStack

[English](./plan.md) | [**简体中文**](./plan.zh-CN.md)

一份一步步追平 OrbStack **速度与体验**的可执行计划。每个里程碑都能独立交付、有可量化的追平目标、并说明如何验证。大致按顺序推进：先把速度/效率的基础打好，再补体验，最后啃文件系统这块最硬的护城河。

> 基线数字来自 `bench.sh`。每个里程碑做完后重跑它来追踪进度。

## 进度（M1–M8）

| | 里程碑 | 状态 |
|---|---|---|
| M1 | golden 镜像 / 秒级重启 | ✅ 已验证 |
| M2 | zram（内核层效率） | ✅ 已验证 |
| M3 | 主动内存 ballooning | ✅ 已验证 |
| M4 | 开发文件系统指南 | ✅ 完成（文档） |
| M5 | 网络 | 🟡 部分 —— DNS 跟随 + 端口转发已端到端验证；`oorb route` + 完整网络栈待做 |
| M6 | Kubernetes（k3s） | ✅ 已验证 —— e2e 套件里 `kubectl get nodes` → Ready |
| M7 | 多机 | ✅ 已验证 —— e2e 套件里 `machine create`+`exec` 通过 |
| M8 | 原生菜单栏 GUI | ✅ 可编译+启动（无头环境没法肉眼看 UI） |
| M9 | 原生级文件系统 | ⛔ 未开始（最硬的护城河） |

## 阶段 0 加固 —— 这轮验证发现并修复了什么

一轮长时间的验证把套件从一团乱变成稳定的 9/12,并定位修复了真正的可靠性 bug（均已修,见 git log）：

1. **二进制签名** —— `oorb` 只在*构建*时给 VZ 二进制签名;任何重新编译都会让它失去签名 → VZ 拒绝启动（`VZErrorDomain Code=2`）。这是"VM 起不来"的核心原因。现在 `bin()` 每次都重新签名。
2. **bash 3.2 空数组崩溃** —— 没启用 k8s 时（常见情况）`oorb start` 在 macOS 自带 bash 上崩溃（`set -u` 下空数组展开）。已用 3.2 安全写法修复。
3. **`machine exec`** —— 之前走宿主机 socket 跑 `docker exec`,劫持流的输出会丢;改为经客户机 agent 执行。
4. **客户机 agent 卡死** —— 负载下 exec 端口会卡（命令无超时泄漏 fd + accept 错误忙循环）。加了命令超时、并发上限、accept 退避、提高 fd 上限。
5. **重头戏 —— 内存 ballooning**（`MemoryManager`）：它把目标压到 `used+384MiB`,空闲时把 4GB 客户机夹到 ~730MiB,于是一波容器启动就把 dockerd 和 agent OOM 杀了。*这就是"一上负载就崩"的根因。* 已修：2GB 下限 + 比例余量 + 滞回（快速放气、缓慢回收）。已验证：以前 ~5 轮就杀死 agent 的 30 容器压测,现在 30 轮全活。

**已知残留：** 冷启动的非确定性 —— 某些启动里 docker/agent 偶尔要 >5 分钟才起来,或容器出网慢约 60 秒。**无法稳定复现**（多数启动正常）,且在宿主负载不高的情况下连续启动越来越差,更像是数小时高频创建/销毁后 VZ/宿主侧的退化,而非代码逻辑 bug。请在干净的宿主状态下（重启）复验。

**更精确的复现（2026-05-26 实测）：** **从 golden 镜像冷启动既快又稳** —— `oorb start` ~8.8 秒就绪,`docker run`、VirtioFS、Rosetta、机器、zram、ballooning、DNS 跟随全过。失败集中在**引导/重启一块被改写过的 `disk.img`**（高频折腾后,或 `k8s enable` 在刚装完 k3s 的盘上做 stop→start）：guest 内核起来了（vsock 会 RST 连接）,但 **dockerd + guest agent 始终没启动**,于是优雅关机无法 ACPI 下电、退化为强杀。另外,有一次 golden 启动里**容器出不了网,但 dockerd 自己 pull 镜像却成功** —— 即 guest 主机有网,但容器走的 docker0 NAT/MASQUERADE 路径没起来,这也连带搞挂端口转发和 k3s CNI。结论：**当下冷 golden 启动可信;阶段 0 未解决的是「在被写过的盘上可靠重启」+「docker0 NAT 确定性拉起」。** `oorb reset`（恢复 golden）是可靠的兜底恢复手段。

**此后的耐久性修复：**
6. **磁盘级加锁**（即上文点名的"下一个修复"）—— 引擎在引导前对磁盘镜像加排他 `flock`（`DiskLock`），两个 VM 再也无法同时写同一镜像（即此前弄坏后续启动的并发写 ext4 损坏）。进程退出时内核自动释放,崩溃也不会留下残留锁。
7. **`oorb start` 快速失败** —— 引擎若提前退出（磁盘被锁、VZ 拒绝引导等）,包装脚本现在会检测到进程已死,立即打印 `vm.log` 里的真实错误,而不是轮询 5 分钟后报一个笼统的超时。

## 重新评估 —— 与 OrbStack 的真实差距

做完 M1–M8 后,判断变了:最大的差距**不再是某个缺失功能**,而是**地基还不够稳 + 一批功能写了但没被证明**:

- **可靠性**:这一轮反复遇到 VM 引导/首次配置超时、强杀弄脏盘、残留构建进程占着 SwiftPM/Go 锁导致连环卡死。OrbStack 开箱即用,而 openorb 有时还"起不来"——这是最痛的实际差距。
- **未验证的工作**:`oorb route`(M5)、k3s(M6)、机器(M7)代码完整但从没端到端跑过——而且 M7 还真藏了个 bug,直到一个迟到的验证任务跑完才暴露。**没跑过的代码不算数。**
- **成熟度**:GUI(M8)只是菜单栏壳;没有打包分发;文件系统护城河(M9)没碰。

差距归为三类:

**A. 地基(稳定性 & 可信度)—— 当前最大短板**
1. 引导/配置可靠性:`oorb start` 必须*总能*起来,关机绝不脏盘,不出现锁连环卡死。
2. 把写好的验通:M5 route、M6 k3s、M7 机器跑出真实端到端绿灯(并修 bug)。
3. 自动化 e2e 测试套件(start→docker→挂载→rosetta→端口→k8s→机器→stop),接 CI,杜绝"未验证的提交"。

**B. 性能(真"快")**
4. 自编译精简内核 + 直引导 → 冷启动从 ~8s 压到 ~1–2s(OrbStack 水平)。
5. **M9 文件系统护城河** —— 自研 virtiofs 缓存/DAX(很可能要在 `Hypervisor.framework` 上自建 VMM)。小文件从 ~21× 干到接近原生。唯一研究级难点。

**C. 产品(真"好用")**
6. 完整 GUI(容器/镜像/卷/日志/机器/k8s/设置),而非菜单栏壳。
7. 网络纵深:VPN *流量*路由(gvproxy)、容器按 IP 直达、`*.oorb.local` 域名。
8. 打包:签名+公证 `.app`、dmg / brew cask、Sparkle 自动更新。

## 推进方案 —— 阶段 0 → 1 → 2

> 相比原来的 M 顺序做了**调整:先夯地基,再冲性能,最后产品**。地基没稳就继续堆功能 = 沙上建塔。

**阶段 0 —— 地基硬化(先做)**
- 0.1 根因排查并修复引导/配置可靠性(EFI 延迟?cloud-init 卡 apt?锁竞争?);目标稳定 <10s 起;关机一律优雅路径;`oorb start` 自检自愈。
- 0.2 `make verify` 一键端到端套件 + CI;任一失败即红。
- 0.3 验通并加固 M5 route / M6 k3s / M7 机器,做到真绿。

**阶段 1 —— 性能**
- 1.1 自编译精简内核 + 直引导 → ~1–2s 启动。
- 1.2(护城河)M9 自研 virtiofs 缓存/DAX → 文件系统接近原生。最难最贵,压轴。

**阶段 2 —— 产品**
- 2.1 完整 SwiftUI 应用(多面板)。
- 2.2 gvproxy 网络栈(VPN 流量 + 容器 IP + 域名)。
- 2.3 签名/公证 `.app` + brew cask + 自动更新。

**结论:** 打平 OrbStack 的*速度* ≈ 阶段 1.2(研究级、数月、很可能要自建 VMM);打平*稳定与精致* ≈ 阶段 0 + 阶段 2(工程量大但不难)。**下一步该做阶段 0,而不是 M9。**

## 追平记分卡

| 维度 | OrbStack | openorb 现状 | 目标里程碑 |
|---|---|---|---|
| CPU 虚拟化 / Rosetta | ✅ | ✅ 已持平 | — |
| Docker / 端口 / 出网 / DNS 解析 | ✅ | ✅ | — |
| 冷启动 | ~1–2 秒 | 首次 ~30–60s / 复用 ~3–6s | **M1、M2** |
| 内存占用 | 动态 + zram | 固定、无 zram | **M3** |
| bind mount 小文件 | ~原生 | 慢约 21× | **M4**(缓解)、**M9**(追平) |
| VPN 流量 / 容器 IP | ✅ | 仅 DNS | **M5** |
| Kubernetes | 一键 | 无 | **M6** |
| 多机 | ✅ | 单 VM | **M7** |
| GUI | 原生应用 | 仅 CLI | **M8** |

---

## M1 — 秒级重启（golden 镜像）
- **目标：** 在已配置好的盘上 `oorb start` **≤ 3 秒**;不再重复 provisioning。
- **任务：** 让 `oorb build-image` 产出完整配置好的 "golden" 盘(启动一次、等 cloud-init 跑完、快照);`oorb start` 直接复用;保留一份纯净副本以便低成本重置。
- **验证：** `time oorb start` ≤ 3 秒;紧接着 `docker run hello-world` 立即可用。
- **工作量：** 🟢 小 · **风险：** 低 · **依赖：** 无。

## M2 — 快速启动 + 自编译内核
- **目标：** 冷启动向 OrbStack 的 ~1–2 秒靠拢;有一个我们可控的内核。
- **任务：** **在客户机内**编译内核(原生 arm64,免去 macOS 交叉编译的坑),单体内核(virtio-blk/net/fs、vsock、ext4、overlay、**zram** 全设 `=y`);用 VZ **直引导**(`--kernel`,已支持)启动而非 EFI;裁掉无用驱动。
- **验证：** `uname -r` 显示自定义内核;`bench.sh` 测启动时间;完整 Docker/VirtioFS/Rosetta/端口转发套件仍通过。
- **工作量：** 🟡 中 · **风险：** 中(配出能启动的 `.config` 可能要试几次) · **依赖：** 无。
- **解锁：** zram 内置(M3)、调优、更快启动。

## M3 — 内存效率（zram + ballooning）
- **目标：** 空闲占用低;像 OrbStack 一样按需回收内存。
- **任务：** 启用 **zram** 交换(M2 内置);在主机侧加一个**主动 ballooning** 循环,读客户机内存压力并调整 VZ 气球目标;可选 **KSM** 做跨容器内存页去重。
- **验证：** 空闲 RSS 明显下降;跑很多小容器不吃满内存。
- **工作量：** 🟡 中 · **风险：** 中 · **依赖：** M2(zram)。

## M4 — 开发文件系统（务实版）
- **目标：** 现在就让"挂源码开发"可用(不等 M9 大改)。
- **任务：** 给 `oorb` 加助手 + 文档,推命名卷工作流(把 `node_modules`/构建目录放客户机本地盘);调研客户机侧任何可用的缓存开关;**给 `bench.sh` 加小文件 + 真实负载(`npm install`)用例**。
- **验证：** 文档化的工作流让 `npm install` 接近原生;bench 显示提升。
- **工作量：** 🟢 小–中 · **风险：** 低 · **依赖：** 无。

## M5 — 无缝网络（用户态网络栈）
- **目标：** VPN **流量**路由、容器/机器按 IP 互达、可选域名。
- **任务：** 集成用户态网络栈(如 **gvisor-tap-vsock / gvproxy**),让客户机流量经 macOS 走(跟随 VPN 路由),配统一 bridge。
- **验证：** 容器能访问只在 VPN 内的主机;macOS 上能 ping 通容器 IP。
- **工作量：** 🔴 大 · **风险：** 中 · **依赖：** 无(DNS 解析已跟随 Mac)。

## M6 — Kubernetes
- **目标：** 一条命令拉起可用集群。
- **任务：** 在客户机跑 **k3s**;投影 kube API(vsock tcp 转发 → `localhost:6443`);写入/合并 kubeconfig;`oorb k8s enable/disable`。
- **验证：** macOS 上 `kubectl get nodes` 并部署一个示例。
- **工作量：** 🟡 中 · **风险：** 低–中 · **依赖：** M5 锦上添花(服务网络)。

## M7 — 多台 Linux 机器
- **目标：** OrbStack 式的命名、多发行版机器,共用内核。
- **任务：** 管理多个 rootfs 镜像;`oorb machine create/list/delete/exec`;每机器独立挂载/网络;一个 SSH/exec 多路复用。
- **验证：** 创建 Ubuntu 和 Alpine 两台机器,分别 exec 进去,文件隔离。
- **工作量：** 🔴 大 · **风险：** 中 · **依赖：** M2(共用自定义内核更顺)。

## M8 — GUI
- **目标：** 原生应用体验,而非纯 CLI。
- **任务：** 一个包裹引擎的 **SwiftUI** 应用——状态/菜单栏、容器、机器、日志、设置、启停;打包 `openorb` 引擎 + 客户机 agent。
- **验证：** 安装 `.app`,从界面管理一切。
- **工作量：** 🔴 大 · **风险：** 低(是工程量,不是研究难题) · **依赖：** M6/M7 让功能面更完整。

## M9 — 护城河：原生级文件系统
- **目标：** 追平 OrbStack 的 ~75–95% 原生 bind mount 速度(现在慢约 21×)。
- **任务：** 最硬的部分——VZ 内置 VirtioFS 不暴露缓存/DAX 调优,所以需要我们**自己的 virtiofs 服务端,带缓存/批处理(可能含 DAX)**。这很可能意味着放弃 VZ 高层 API,改用 **`Hypervisor.framework` 上的自建 VMM**(以掌控设备模型)。这是最深、最贵的一块——OrbStack 真正的护城河。
- **验证：** `bench.sh` 小文件落在本地盘的 ~2× 以内;bind mount 上 `npm install` 接近原生。
- **工作量：** 🔴🔴 非常大(研究级难度,数月) · **风险：** 高 · **依赖：** 最好在 M1–M8 之后。

---

## 里程碑 vs 阶段

上面的 M1–M9 是最初的功能拆解(M1–M8 已建好,见「进度」)。而**阶段 0 → 1 → 2** 才是经过重新评估后**接下来真正执行的计划**:阶段 0 把"建好了但不稳"的地基变可信,阶段 1(含 M9)冲真速度,阶段 2 补产品面。OrbStack 深入分析见[调研报告](../orbstack-research.md)。
